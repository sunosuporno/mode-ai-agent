import {
  createTool
} from "./chunk-MGBJXP6Y.mjs";
import {
  __name
} from "./chunk-PAWJFY3S.mjs";

// src/classes/WalletClientBase.ts
import { z } from "zod";
var WalletClientBase = class {
  static {
    __name(this, "WalletClientBase");
  }
  getCoreTools() {
    return [
      createTool({
        name: "get_address",
        description: "Get the address of the wallet",
        parameters: z.object({})
      }, () => this.getAddress()),
      createTool({
        name: "get_chain",
        description: "Get the chain of the wallet",
        parameters: z.object({})
      }, () => this.getChain()),
      createTool({
        name: "get_balance",
        description: "Get the balance of the wallet",
        parameters: z.object({
          address: z.string()
        })
      }, (parameters) => this.balanceOf(parameters.address))
    ];
  }
};

export {
  WalletClientBase
};
//# sourceMappingURL=chunk-S4E3EJMS.mjs.map