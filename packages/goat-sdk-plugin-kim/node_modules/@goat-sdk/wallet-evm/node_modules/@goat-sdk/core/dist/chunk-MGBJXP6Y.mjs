import {
  __name
} from "./chunk-PAWJFY3S.mjs";

// src/classes/ToolBase.ts
var ToolBase = class {
  static {
    __name(this, "ToolBase");
  }
  /** The name of the tool */
  name;
  /** A description of what the tool does */
  description;
  /** The Zod schema defining the parameters, that will be passed to the tool's execute method */
  parameters;
  /**
   * Creates a new Tool instance
   * @param config - The configuration object for the tool
   */
  constructor(config) {
    this.name = config.name;
    this.description = config.description;
    this.parameters = config.parameters;
  }
};
function createTool(config, execute) {
  return new class extends ToolBase {
    execute(parameters) {
      return execute(parameters);
    }
  }(config);
}
__name(createTool, "createTool");

export {
  ToolBase,
  createTool
};
//# sourceMappingURL=chunk-MGBJXP6Y.mjs.map